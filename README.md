# Fake Device Client (Python Technical Exercise for Yoto)

Small Python project that simulates a hardware device (`FakeDevice`) and a client wrapper (`DeviceClient`).

## Setup (uv)

Install uv:
- `brew install uv`

Clone and install dependencies:
```bash
git clone git@github.com:YujinMatsutoya/python-technical-exercise.git
cd python-technical-exercise
uv sync

